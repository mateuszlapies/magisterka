import Sync from "./components/Sync";

const AppRoutes = [
  {
    index: true,
    element: <Sync />
  }
  //{
  //  path: '/counter',
  //  element: <Counter />
  //}
];

export default AppRoutes;
